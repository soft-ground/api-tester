{
  "name": "api-tester-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop (Windows/macOS) shell for API Tester — runs the Nest server + UI locally, no Docker.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:server": "npm --prefix ../server run build",
    "build:web": "npm --prefix ../web run build",
    "build:deps": "npm run build:server && npm run build:web",
    "prep:server": "node scripts/prep-server.mjs",
    "start": "electron .",
    "dev": "npm run build && electron .",
    "pack": "npm run build && npm run prep:server && electron-builder --dir",
    "dist": "npm run build && npm run prep:server && electron-builder"
  },
  "devDependencies": {
    "electron": "^31.3.0",
    "electron-builder": "^24.13.3",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "electron-updater": "^6.8.9",
    "embedded-postgres": "^18.4.0-beta.17"
  }
}
